import React from "react";
import { RateInventorySection } from "@/components/sections/rate-inventory/RateInventorySection";

export const metadata = {
    title: "Rate & Inventory Control | My Tara PMS",
    description: "Monitor ketersediaan kamar fisik riil dan kelola master harga Rate Plans untuk hotel dan integrasi Channel Manager."
};

export default function RateInventoryPage() {
    return <RateInventorySection />;
}
