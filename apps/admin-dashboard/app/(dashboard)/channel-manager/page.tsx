import React from "react";
import { ChannelManagerSection } from "@/components/sections/channel-manager/ChannelManagerSection";

export const metadata = {
    title: "Channel Manager & Channex Integration | My Tara PMS",
    description: "Kelola integrasi 68+ OTA global, Channex API, dan pemetaan harga & ketersediaan kamar."
};

export default function ChannelManagerPage() {
    return <ChannelManagerSection />;
}

